# Чек-лист первой сборки RRS (после больших сессий 2026-08-19..22)

Ни одна строка этих сессий не компилировалась (в среде нет компилятора).
Все правки статически выверены; ожидайте мелочей уровня include, не
структурных проблем. Порядок важен.

## 1. Конфигурация
- [ ] re-run CMake (GLOB-списки: новые файлы подхватятся только так):
      новые модули `simulator/cassette`, файлы route_editor2 (TrackProfile,
      TrajectoryPicker, Gizmo, KeyBindings, MeasureTool, Validator,
      TrackFurniture, SplineTool, команды PasteObjects/ToggleVisibility/
      ReplaceLabel/ResetScale/PastePrefab), rolling-stock-creator
      (BlendImporter, TractionChart, NewVehicleWizard), graphics
      (postprocess.cpp/h добавлены в CMake явно).
- [ ] Требования: Qt6 (Core/Widgets/Xml), vsg 1.1.13, vsgXchange,
      vsgImGui, Jolt (FetchContent), Python — не нужен.

## 2. Сборка (ожидаемые точки правок)
- [ ] simulator: vehicle (steam/pantograph/depot/ER-source), cassette,
      model (cassette+ключи), collision, player (CfgReader).
- [ ] viewer: новый include-путь player/collision; UpdateViewerHandler
      (walk), CameraWalkManipulator, MyGui (notice), VehiclesHandler.
- [ ] graphics-core/graphics: пресет Extreme, ACES, postprocess.
- [ ] addons/vl60 (vl60k+vl60pk): автоостанов (порт апстрима, базы
      патчей совпадали до строк).
- [ ] route_editor2, tools/rolling-stock-creator.

## 3. Первый запуск — дымовой тест
- [ ] Сервер+клиент стартуют, маршрут грузится.
- [ ] F7 — пешая камера (мир коллизий строится лениво, лог «Walk mode:
      loaded N collision bodies»); Ctrl+Enter у двери кабины — вход,
      из кабины — выход к ExitPos; попап «Нажмите Enter...» не мешает.
- [ ] Кассеты: Ctrl+R — красный попап «Запись параметров движения
      начата», файл в Cassettes/; повторно — «окончена»; открыть
      tools/cassette-decoder/index.html + test-cassette.kr (12 каналов,
      УР опережает ТМ).
- [ ] Депо: зона service.conf с Resources="power", на стоянке K — кабель,
      L — колонка, O — вводной; движение рвёт кабель (журнал [DEPOT]).
- [ ] ВЛ60к: поднять токоприёмники — Uks не ноль (мост pantograph),
      рекуперация живая; Alt+T — автоостанов (тумблеры вниз, ЭПК выкл,
      ключ в замке), Alt+R — автозапуск.
- [ ] Графика: настройки → пресеты: Legacy (старый вид), High/Ultra
      (PBR/ACES/тени; перезапуск), Extreme (bloom/SSAO/туман; сцена в
      цепочке без MSAA — это ожидаемо).
- [ ] Редакторы: route_editor2 (P — пути, окно «Путь» уклон ‰, кнопка
      «Сгенерировать путь» — рельсы/шпалы/балласт; N — сплайн; G —
      гизмо; Validator), rolling-stock-creator (мастер Файл→..., вкладки
      Анимации/Звук, .blend через Blender, экспорт пакета).

## 4. Верификация без запуска
- [ ] `_verify/*.py` — все зеркала (~250 проверок) должны быть зелёными:
      python -u _verify\verify_walking.py (и т.д. 20+ скриптов).

## 5. Известные ограничения (не баги)
- SSAO-флаг High-тира декоративен (в разработке), рабочий — в Extreme.
- Ресайз окна на Extreme требует перезапуска (offscreen-таргеты).
- Паровая тяга: система готова, но аддона-паровоза в дереве нет.
- УР-канал кассеты привязан у ВЛ60к; для vl60pk — 2 строки по образцу
  (initBrakesControl + setBrakeERSource).
- Пешый режим: тела ПС — боксы 15×1.6×2 м (грубая аппроксимация).
